runtime: php
env: flex

handlers:
- url: /(.*\.(gif|png|jpg|css|js)(|\.map))$
  static_files: dist/\1
  upload: dist/(.*)(|\.map)

- url: /(.*)
  static_files: dist/index.html
  upload: dist/index.html

env_variables:
  MYSQL_USER: dbuser
  MYSQL_PASSWORD: 007ShakenNotStirred%
  MYSQL_DSN: mysql:dbname=pennapps;unix_socket=/cloudsql/pennapps-179414:us-east1:studentoverflowdb

beta_settings:
  cloud_sql_instances: "pennapps-179414:us-east1:studentoverflowdb"

runtime_config:
  document_root: .
